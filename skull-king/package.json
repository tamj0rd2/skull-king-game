{
  "name": "skull-king",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "test:watch": "node --test --watch ./",
    "server": "node ./src/server.js"
  },
  "watch": {
    "server": {
      "patterns": ["./src/{server,client}.js"]
    },
    "client": {
      "patterns": ["./src/{server,client}.js"]
    }
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.0.3",
    "@types/node": "^20.2.5",
    "npm-watch": "^0.11.0",
    "svelte": "^3.57.0",
    "vite": "^4.3.2",
    "ws": "^8.13.0"
  }
}
